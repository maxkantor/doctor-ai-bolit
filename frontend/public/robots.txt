User-agent: *
Allow: /

# Admin dashboard — not for indexing
Disallow: /admin/

# Chat session URLs are private; index /chat only (listed in sitemap)
Disallow: /chat?session=

# Crawl-delay is not used by Googlebot; omit to avoid confusing other bots

Sitemap: https://doctoraibolit.com/sitemap.xml
